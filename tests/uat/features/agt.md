# UAT Feature: `mc agent` (AGT)

**Command:** `mc agent <subcommand>` / `mc agt <subcommand>`
**Source:** `src/mc/cli/commands/agent.py`, `src/mc/runtime.py`
**Prefix:** AGT

**Pre-conditions (all TCs):**
- `mc` installed via `uv tool install` inside a container
- `MC_RUNTIME_MODE=agent` environment variable set
- Valid case workspace initialized

> **Status:** No TCs yet. Use `/uat-review agt` to generate initial test suite.

---

## Coverage Map

| Behavior | TC |
|---|---|
| *(none yet)* | — |
