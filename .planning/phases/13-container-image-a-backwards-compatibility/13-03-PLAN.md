---
phase: 13-container-image-a-backwards-compatibility
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - tests/integration/test_backwards_compatibility.py
autonomous: false

must_haves:
  truths:
    - "All v1.0 commands exist and return expected help output"
    - "Command syntax unchanged from v1.0"
    - "Workspace structure compatible with containers"
    - "Configuration files readable by both host and container"
  artifacts:
    - path: "tests/integration/test_backwards_compatibility.py"
      provides: "Comprehensive backwards compatibility test suite"
      min_lines: 120
  key_links:
    - from: "tests/integration/test_backwards_compatibility.py"
      to: "mc CLI commands"
      via: "subprocess.run(['mc', command])"
      pattern: "subprocess\\.run.*\\['mc'"
---

<objective>
Validate comprehensive backwards compatibility by testing all v1.0 commands work unchanged on the host, and verify config/workspace compatibility between host and container contexts.

Purpose: Guarantee existing workflows continue to function, prove v2.0 is purely additive, validate cross-context compatibility
Output: Complete backwards compatibility test suite with human verification checkpoint
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-container-image-a-backwards-compatibility/13-CONTEXT.md
@.planning/phases/13-container-image-a-backwards-compatibility/13-RESEARCH.md
@src/mc/cli/main.py
</context>

<tasks>

<task type="auto">
  <name>Create comprehensive backwards compatibility test suite</name>
  <files>
    tests/integration/test_backwards_compatibility.py
  </files>
  <action>
Create exhaustive backwards compatibility test suite covering all v1.0 commands and cross-context compatibility:

**Test infrastructure:**
- Import subprocess, pytest, pathlib
- Helper function: `run_cli_command(args: List[str]) -> subprocess.CompletedProcess`
  - Runs `mc` + args with 30s timeout
  - Captures stdout/stderr as text
  - Returns CompletedProcess for assertion
- Use pytest class structure for organization

**Class 1: TestV1Commands - V1.0 command syntax tests**

**1. attach command:**
- `test_attach_command_exists()`: mc attach --help returns 0, mentions "Download attachments"
- `test_attach_requires_case_number()`: mc attach without args returns non-zero
- Test both --serial and --quiet flags exist

**2. check command:**
- `test_check_command_exists()`: mc check --help returns 0, mentions "Check the state of a workspace"
- `test_check_requires_case_number()`: mc check without args returns non-zero
- Test --fix flag exists

**3. create command:**
- `test_create_command_exists()`: mc create --help returns 0, mentions "Create a workspace"
- `test_create_requires_case_number()`: mc create without args returns non-zero
- Test --download flag exists

**4. case-comments command:**
- `test_case_comments_command_exists()`: mc case-comments --help returns 0
- `test_case_comments_requires_case_number()`: mc case-comments without args returns non-zero

**5. ls command (LDAP search):**
- `test_ls_command_exists()`: mc ls --help returns 0, mentions "Search for a user in LDAP"
- `test_ls_requires_uid()`: mc ls without args returns non-zero
- Test --all flag exists

**6. go command:**
- `test_go_command_exists()`: mc go --help returns 0
- `test_go_requires_case_number()`: mc go without args returns non-zero
- Test --launch flag exists

**7. Global flags:**
- `test_version_flag()`: mc --version outputs version string
- `test_debug_flag()`: mc --debug recognized (doesn't fail)
- `test_json_logs_flag()`: mc --json-logs recognized
- `test_debug_file_flag()`: mc --debug-file PATH recognized

**Class 2: TestV2Commands - New container commands**

**8. New v2.0 commands (validate they exist but don't break v1.0):**
- `test_container_commands_exist()`: mc list, mc stop, mc delete, mc exec --help all work
- `test_quick_access_pattern()`: mc <8-digit-number> pattern recognized

**Class 3: TestWorkspaceCompatibility - Workspace and config tests**

**9. Workspace structure test:**
- `test_workspace_structure_unchanged()`: Verify existing workspace directories mount without migration
- Create dummy workspace with known files
- Verify files accessible after container mount
- Ensure no breaking changes to workspace layout

**10. Config compatibility test (COMPAT-03):**
- `test_config_readable_in_container()`: Verify config file readable from inside container
- Create test container using ContainerManager
- Execute command to read config file from container
- Parse config content and verify expected fields present (e.g., base_directory)
- Verify same config accessible on host
- Proves "Configuration files readable by both host and container mc"

**Testing approach per research:**
- Focus on command existence and syntax, not full execution (avoid Salesforce/LDAP calls)
- Test help output and argument parsing
- Test flag existence
- Don't test actual functionality (that's covered by unit/integration tests)
- Follow research example pattern from test_backwards_compatibility.py code sample

**Markers and organization:**
- @pytest.mark.integration decorator
- @pytest.mark.backwards_compatibility custom marker
- Class-based organization: TestV1Commands, TestV2Commands, TestWorkspaceCompatibility
- Skip conditions if mc not installed: @pytest.mark.skipif(not shutil.which('mc'))
  </action>
  <verify>
    pytest tests/integration/test_backwards_compatibility.py -v -m backwards_compatibility
  </verify>
  <done>
Backwards compatibility test suite created with 22+ tests covering all v1.0 commands, v2.0 commands, workspace compatibility, and config compatibility (COMPAT-03), validates no breaking changes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Comprehensive backwards compatibility test suite validating all v1.0 commands work unchanged and cross-context compatibility
  </what-built>
  <how-to-verify>
**Automated verification:**
1. Run full test suite:
   ```
   pytest tests/integration/test_backwards_compatibility.py -v
   ```
   Expected: All tests pass, 22+ tests executed

**Manual verification of critical workflows:**
2. Test create workflow (v1.0 baseline):
   ```
   mc create 12345678
   mc check 12345678
   ```
   Expected: Workspace created at expected location, check shows valid status

3. Test attach workflow:
   ```
   mc attach 12345678 --help
   ```
   Expected: Help text displays, mentions attachments

4. Test LDAP search:
   ```
   mc ls testuser --help
   ```
   Expected: Help text displays, mentions LDAP search

5. Test global flags:
   ```
   mc --version
   mc --help
   ```
   Expected: Version displays, help text shows all commands

6. Verify v2.0 commands coexist:
   ```
   mc list --help
   mc exec --help
   ```
   Expected: Container commands exist but don't break v1.0 workflows

**Configuration compatibility:**
7. Verify config file readable on host:
   ```
   cat ~/.config/mc/config.toml
   ```
   Expected: Config file format unchanged, no migration needed

8. Verify config readable in container (COMPAT-03):
   ```
   mc create 12345678
   mc exec 12345678 cat /path/to/config  # adjust path to actual mount
   ```
   Expected: Config content identical to host version

**Workspace compatibility:**
9. Verify workspace structure:
   ```
   ls ~/Cases/12345678  # or configured base_directory
   ```
   Expected: Existing v1.0 workspaces still accessible

**What to look for:**
- No command removed or renamed
- No flags changed or removed
- No error messages on help output
- Clean exit codes (0 for success, non-zero for missing args)
- v1.0 and v2.0 commands coexist without conflicts
- Config accessible in both host and container contexts
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass and manual verification succeeds, or describe any compatibility issues found
  </resume-signal>
</task>

</tasks>

<verification>
**Automated test coverage:**
- All v1.0 commands tested for existence
- Help output validated for expected content
- Required arguments validated
- Optional flags validated
- Exit codes checked
- Config and workspace compatibility validated

**Manual verification:**
- Critical workflows tested end-to-end
- No command syntax changes
- Clean error messages
- V1.0 and v2.0 commands coexist
- Config readable in both contexts (COMPAT-03)
</verification>

<success_criteria>
**Measurable completion:**
1. pytest tests/integration/test_backwards_compatibility.py passes with 22+ tests
2. All v1.0 commands return 0 exit code with --help flag
3. No commands removed, renamed, or changed required arguments
4. Config file readable from both host and container (COMPAT-03 validated)
5. Manual verification checkpoint approved by user
6. Existing workspace structure works without migration
</success_criteria>

<output>
After completion, create `.planning/phases/13-container-image-a-backwards-compatibility/13-03-SUMMARY.md`
</output>
