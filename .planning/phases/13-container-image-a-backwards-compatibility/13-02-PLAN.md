---
phase: 13-container-image-a-backwards-compatibility
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/mc/container/manager.py
  - tests/integration/test_container_image.py
autonomous: true

must_haves:
  truths:
    - "Container created from mc-rhel10 image with workspace mounted"
    - "Container has correct environment variables (CASE_NUMBER, CUSTOMER_NAME)"
    - "Shell prompt displays case number in [case-XXXXXXXX] format"
    - "MC CLI accessible inside container"
  artifacts:
    - path: "src/mc/container/manager.py"
      provides: "Updated ContainerManager.create() to use mc-rhel10 image"
      contains: "mc-rhel10:latest"
    - path: "tests/integration/test_container_image.py"
      provides: "Container image integration tests"
      min_lines: 50
  key_links:
    - from: "src/mc/container/manager.py"
      to: "container image mc-rhel10:latest"
      via: "podman_client.images.get()"
      pattern: "mc-rhel10:latest"
    - from: "ContainerManager.create()"
      to: "container environment"
      via: "environment parameter"
      pattern: "environment=.*CASE_NUMBER"
---

<objective>
Update ContainerManager to use mc-rhel10 image and validate complete container lifecycle with mounted workspace, environment variables, and shell customization.

Purpose: Integrate new RHEL 10 image into container orchestration, prove end-to-end workflow
Output: Working containers from mc-rhel10 image with case metadata and shell customization
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-container-image-a-backwards-compatibility/13-CONTEXT.md
@.planning/phases/11-container-lifecycle-a-state-management/11-02-SUMMARY.md
@src/mc/container/manager.py
@container/Containerfile
@container/entrypoint.sh
</context>

<tasks>

<task type="auto">
  <name>Update ContainerManager to use mc-rhel10 image</name>
  <files>
    src/mc/container/manager.py
  </files>
  <action>
Update ContainerManager.create() method to use mc-rhel10:latest image instead of placeholder:

**Image specification:**
- Change image parameter from current value to "mc-rhel10:latest"
- Image must exist (built by 13-01 plan)
- Add fallback error if image not found with helpful message: "Run 'podman build -t mc-rhel10:latest -f container/Containerfile .' first"

**Environment variables (expand existing environment dict):**
- CASE_NUMBER: str(case_number)
- CUSTOMER_NAME: metadata.customer_name if available, else "Unknown"
- WORKSPACE_PATH: "/case"
- MC_RUNTIME_MODE: "agent" (ensure runtime mode detection works)
- Preserve any existing environment variables from Phase 11

**Container configuration (verify existing):**
- Workspace mount: workspace_path:/case:U (Phase 11 already implemented)
- Config mount: config read-only mount (Phase 10 already implemented)
- Userns: --userns=keep-id (Phase 11 already implemented)
- Auto-restart: true (Phase 11 already implemented)

**BASH_ENV for custom bashrc (if not already set):**
- Check if Phase 12 bashrc integration already in place
- If bashrc file generated, set BASH_ENV environment variable to bashrc path
- Bashrc should be in container at /tmp/bashrc or similar ephemeral location

**Error handling:**
- Catch image not found error from podman
- Provide actionable error message with build command
- Don't fail silently

**Preserve existing functionality:**
- Don't break Phase 11 container lifecycle operations
- Maintain backward compatibility with existing tests
- Only change image reference and environment variables
  </action>
  <verify>
    pytest tests/unit/test_container_manager_create.py -v
    pytest tests/integration/test_container_create_integration.py -v
  </verify>
  <done>
ContainerManager.create() updated to use mc-rhel10:latest image, sets correct environment variables including MC_RUNTIME_MODE=agent, existing tests pass
  </done>
</task>

<task type="auto">
  <name>Create container image integration tests</name>
  <files>
    tests/integration/test_container_image.py
  </files>
  <action>
Create comprehensive integration tests for container image functionality:

**Test setup:**
- Pytest fixtures for temporary workspace directory
- Fixture for ContainerManager instance
- Fixture for cleanup (stop and remove test containers)
- Mock case metadata with case_number, customer_name

**Test: Image exists and is buildable**
- Skip if image doesn't exist with helpful message
- Verify podman can see mc-rhel10:latest image
- Check image has expected labels or tags

**Test: Container creation with mc-rhel10 image**
- Create container using ContainerManager.create()
- Verify container created successfully
- Verify container uses mc-rhel10:latest image
- Verify workspace mounted at /case
- Clean up container after test

**Test: Environment variables set correctly**
- Create container with case_number and metadata
- Execute command inside container: `env | grep MC_`
- Verify MC_RUNTIME_MODE=agent
- Verify CASE_NUMBER matches input
- Verify CUSTOMER_NAME set
- Verify WORKSPACE_PATH=/case

**Test: Shell prompt customization**
- Create container with case_number=12345678
- Execute command: `bash -c 'echo $PS1'`
- Verify PS1 contains "case-12345678"
- Matches CONTEXT.md requirement format

**Test: MC CLI accessible inside container**
- Create container
- Execute: `python3 -c "import mc; print(mc.version.get_version())"`
- Verify import succeeds and version prints
- Proves mc CLI installed correctly

**Test: Essential tools available**
- Create container
- Execute: `which vim && which curl && which openssl`
- Verify all tools found
- Tests CONTEXT.md tool requirements

**Test: Runtime mode detection inside container**
- Create container
- Execute: `python3 -c "from mc.runtime import get_runtime_mode; assert get_runtime_mode() == 'agent'"`
- Verify mode detected as "agent" inside container
- Proves environment variable propagation

**Integration test markers:**
- Use @pytest.mark.integration decorator
- Use @pytest.mark.skipif for conditional skip when image not built
- Proper cleanup with try/finally or fixtures
  </action>
  <verify>
    pytest tests/integration/test_container_image.py -v -m integration
  </verify>
  <done>
Container image integration tests created, verify mc-rhel10 image functionality end-to-end, all tests pass when image built
  </done>
</task>

</tasks>

<verification>
**ContainerManager integration:**
- Existing container lifecycle tests pass (no regressions)
- New environment variables present in created containers
- Image reference correctly updated to mc-rhel10:latest

**Integration test coverage:**
- Container creation with new image succeeds
- Environment variables propagate correctly
- Shell prompt customization works
- MC CLI accessible and imports successfully
- Essential tools available
- Runtime mode detected as "agent"
</verification>

<success_criteria>
**Measurable completion:**
1. pytest tests/unit/test_container_manager_create.py passes (no regressions)
2. pytest tests/integration/test_container_image.py passes with 7+ tests
3. Create real container and verify: `mc create 12345678` followed by manual container inspection
4. Exec into container shows PS1 prompt in correct format: [case-12345678]$
5. Inside container, `python3 -c "from mc.runtime import is_agent_mode; assert is_agent_mode()"` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-container-image-a-backwards-compatibility/13-02-SUMMARY.md`
</output>
