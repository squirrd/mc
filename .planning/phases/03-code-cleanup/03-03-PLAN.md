---
phase: 03-code-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mc/cli/main.py
  - src/mc/cli/commands/case.py
  - src/mc/controller/workspace.py
  - tests/unit/test_workspace.py
  - tests/integration/test_ldap.py
autonomous: true

must_haves:
  truths:
    - "All user-facing typos corrected"
    - "CLI flag naming follows conventions (--all lowercase)"
    - "Status output displays 'CheckStatus' not 'CheckStaus'"
    - "Attachment download message spelled correctly"
    - "Tests updated to match corrected strings"
  artifacts:
    - path: "src/mc/cli/main.py"
      provides: "Corrected --all flag"
      contains: "--all"
    - path: "src/mc/cli/commands/case.py"
      provides: "Corrected attachment message"
      contains: "downloading attachments"
    - path: "src/mc/controller/workspace.py"
      provides: "Corrected CheckStatus"
      contains: "CheckStatus"
  key_links:
    - from: "tests/unit/test_workspace.py"
      to: "src/mc/controller/workspace.py"
      via: "assertions match corrected output"
      pattern: "CheckStatus"
    - from: "tests/"
      to: "CLI output"
      via: "test assertions validate corrected strings"
      pattern: "downloading attachments"
---

<objective>
Fix all typos in CLI flags, status classes, and user-facing output across codebase and tests.

Purpose: Professional user experience, correct CLI conventions, eliminate technical debt
Output: Typo-free codebase with updated tests
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-cleanup/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CLI flag and status typos in source code</name>
  <files>
    src/mc/cli/main.py
    src/mc/cli/commands/case.py
    src/mc/controller/workspace.py
  </files>
  <action>
Fix BUG-01 in src/mc/cli/main.py (line 51):
- Change `parser_ls.add_argument('-A', '--All', action='store_true')`
- To: `parser_ls.add_argument('-A', '--all', action='store_true')`
- This follows argparse convention of lowercase long flags

Fix DEBT-05 in src/mc/cli/commands/case.py (line 102):
- Change `print(f" and dowloading attachemnts for case: {case_number}")`
- To: `print(f" and downloading attachments for case: {case_number}")`

Fix BUG-02 in src/mc/controller/workspace.py (line 87):
- Change `print(f"CheckStaus: {status}")`
- To: `print(f"CheckStatus: {status}")`

Fix BUG-02 in src/mc/cli/commands/case.py (line 125):
- Change `print(f"CheckStaus: {status}")`
- To: `print(f"CheckStatus: {status}")`

Verify no other occurrences:
```bash
grep -r "CheckStaus" src/
grep -r "dowloading" src/
grep -r "attachemnts" src/
grep -r "--All" src/
```

All should return no results after fixes.
  </action>
  <verify>
# Verify typos removed
! grep -r "CheckStaus" src/
! grep -r "dowloading" src/
! grep -r "attachemnts" src/
! grep -r "'--All'" src/

# Verify corrections present
grep -r "CheckStatus" src/mc/controller/workspace.py
grep -r "downloading attachments" src/mc/cli/commands/case.py
grep -r "'--all'" src/mc/cli/main.py
  </verify>
  <done>
- --All changed to --all in main.py
- "dowloading attachemnts" changed to "downloading attachments" in case.py
- "CheckStaus" changed to "CheckStatus" in workspace.py and case.py
- No typos remain in source code
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests to match corrected strings</name>
  <files>
    tests/unit/test_workspace.py
    tests/integration/test_ldap.py
  </files>
  <action>
Search for hardcoded typo strings in test files:
```bash
grep -r "CheckStaus" tests/
grep -r "dowloading" tests/
grep -r "attachemnts" tests/
grep -r "--All" tests/
```

For each occurrence found:

In test_workspace.py:
- If tests assert on "CheckStaus" output, change to "CheckStatus"
- Update any captured_output or print validation to expect corrected spelling

In test_ldap.py:
- If tests use --All flag, change to --all
- Update any CLI invocation tests to use corrected flag

In any test files with attachment download tests:
- Update assertions expecting "dowloading attachemnts" to "downloading attachments"

If no tests currently validate these strings (tests may mock these functions), document this and ensure future tests use correct spelling.

Run tests after changes to verify they still pass:
```bash
pytest tests/unit/test_workspace.py -v
pytest tests/integration/test_ldap.py -v
```
  </action>
  <verify>
# Verify no typos in tests
! grep -r "CheckStaus" tests/
! grep -r "dowloading" tests/
! grep -r "attachemnts" tests/
! grep -r "'--All'" tests/

# Run affected tests
pytest tests/unit/test_workspace.py -v
pytest tests/integration/test_ldap.py -v
  </verify>
  <done>
- All test assertions updated to expect corrected strings
- Tests using --All flag updated to --all
- Tests expecting "CheckStaus" updated to "CheckStatus"
- Tests expecting typo'd attachment message updated
- All affected tests pass with corrected assertions
  </done>
</task>

<task type="auto">
  <name>Task 3: Run codespell and fix any additional typos found</name>
  <files>
    (varies based on codespell findings)
  </files>
  <action>
Install codespell if not already in dev dependencies:
```bash
pip install codespell
```

Run codespell on source and test directories:
```bash
codespell src/ tests/ --skip="*.pyc,__pycache__,.git" --quiet-level=2
```

If codespell finds additional typos:
- Review each finding for false positives
- Apply fixes to source files
- Update corresponding tests if needed
- Re-run codespell to verify clean

If no additional typos found, document this in commit message.

Common false positives to ignore:
- Variable names (UID, API keys)
- Domain-specific terms (LDAP attribute names)
- Proper nouns (product names, company names)

Create .codespell-ignore file if needed for legitimate exceptions.
  </action>
  <verify>
codespell src/ tests/ --skip="*.pyc,__pycache__,.git" --quiet-level=2
echo "Exit code should be 0 (no typos found): $?"
  </verify>
  <done>
- codespell run on entire codebase
- Any additional typos found and fixed
- .codespell-ignore created if needed for false positives
- Codebase passes codespell validation with zero findings
  </done>
</task>

</tasks>

<verification>
Run full test suite to ensure typo fixes don't break functionality:
```bash
pytest tests/ -v
```

Verify typos removed:
```bash
codespell src/ tests/
grep -r "CheckStaus" src/ tests/  # Should return nothing
grep -r "dowloading" src/ tests/  # Should return nothing
grep -r "'--All'" src/ tests/  # Should return nothing
```

Test CLI commands with corrected flags:
```bash
mc ls testuser --all  # Should work (was --All)
mc --help | grep "all"  # Should show --all flag
```
</verification>

<success_criteria>
- All known typos fixed (CheckStaus, dowloading attachemnts, --All)
- Tests updated to expect corrected strings
- Full test suite passes with updated assertions
- codespell finds no additional typos
- CLI flag follows conventions (--all lowercase)
- User-facing output displays professional spelling
- Requirements DEBT-05, BUG-01, BUG-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-cleanup/03-03-SUMMARY.md`
</output>
