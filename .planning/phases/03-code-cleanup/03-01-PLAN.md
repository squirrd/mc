---
phase: 03-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mc/config/manager.py
  - src/mc/config/models.py
  - src/mc/config/wizard.py
  - src/mc/cli/main.py
  - src/mc/utils/auth.py
  - pyproject.toml
  - tests/unit/test_config.py
autonomous: true

must_haves:
  truths:
    - "Tool uses TOML config file exclusively (no environment variables)"
    - "Config file created via interactive wizard on first run"
    - "Tool fails immediately if legacy env vars detected"
    - "Error messages provide shell-specific unset instructions"
    - "Config file location follows OS conventions (XDG on Linux, etc.)"
  artifacts:
    - path: "src/mc/config/manager.py"
      provides: "Config file I/O with platformdirs"
      min_lines: 50
    - path: "src/mc/config/models.py"
      provides: "Config data schema"
      min_lines: 20
    - path: "src/mc/config/wizard.py"
      provides: "Interactive setup wizard"
      min_lines: 40
    - path: "tests/unit/test_config.py"
      provides: "Config system tests"
      min_lines: 80
  key_links:
    - from: "src/mc/cli/main.py"
      to: "src/mc/config/manager.py"
      via: "load_config() call at startup"
      pattern: "load_config\\(\\)"
    - from: "src/mc/cli/main.py"
      to: "legacy env var detection"
      via: "check before config load"
      pattern: "MC_BASE_DIR.*environ"
    - from: "src/mc/config/manager.py"
      to: "platformdirs"
      via: "user_config_dir() for cross-platform paths"
      pattern: "user_config_dir"
---

<objective>
Migrate from environment variable configuration to TOML config file system with interactive wizard and legacy detection.

Purpose: Consolidate configuration management, improve portability, provide better user experience with interactive setup
Output: Working TOML config system with migration from environment variables
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-cleanup/03-CONTEXT.md
@.planning/phases/03-code-cleanup/03-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install config dependencies and create config modules</name>
  <files>
    pyproject.toml
    src/mc/config/manager.py
    src/mc/config/models.py
    src/mc/config/wizard.py
  </files>
  <action>
Add dependencies to pyproject.toml:
- tomli-w (for writing TOML)
- platformdirs (for cross-platform config paths)
- Add tomli to dependencies list with version constraint (for Python 3.8-3.10 compatibility)

Create src/mc/config/manager.py:
- ConfigManager class with get_config_path() using platformdirs.user_config_dir("mc")
- load_config() method using tomllib (3.11+) or tomli (3.8-3.10) in binary mode
- save_config() method using tomli_w in binary mode
- exists() method to check if config file exists
- Create config directory with parents=True, exist_ok=True

Create src/mc/config/models.py:
- Config data structure as dict schema
- Default config with base_directory: "~/mc" and api.offline_token placeholder
- Config validation function

Create src/mc/config/wizard.py:
- run_setup_wizard() function
- Interactive prompts with defaults (base_directory defaults to ~/mc)
- Require offline_token input (loop until provided)
- Return config dict matching models.py schema

Follow Python 3.11+ tomllib import pattern from RESEARCH.md:
```python
import sys
if sys.version_info >= (3, 11):
    import tomllib
else:
    import tomli as tomllib
```
  </action>
  <verify>
pytest tests/unit/test_config.py -v (after Task 2)
python -c "from mc.config.manager import ConfigManager; from mc.config.wizard import run_setup_wizard; print('imports ok')"
  </verify>
  <done>
- platformdirs and tomli-w added to pyproject.toml dependencies
- ConfigManager class created with load/save/exists methods
- Config schema defined in models.py
- Interactive wizard created in wizard.py
- All imports work without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add legacy env var detection and migration in main.py</name>
  <files>
    src/mc/cli/main.py
  </files>
  <action>
Add check_legacy_env_vars() function at top of main.py before main():
- Check for MC_BASE_DIR and RH_API_OFFLINE_TOKEN in os.environ
- If any found, detect shell from $SHELL env var
- If "fish" in shell, generate "set -e VAR" commands
- Otherwise (bash/zsh), generate "unset VAR" commands
- Print error with:
  * List of detected legacy vars
  * Migration message
  * Shell-specific unset commands
  * Instruction to run wizard
- Call sys.exit(1)

Update main() function:
- Call check_legacy_env_vars() FIRST before any other logic
- Remove hardcoded base_dir line (line 13)
- Remove offline_token env var check (lines 21-24)
- Add config loading:
  ```python
  from mc.config.manager import ConfigManager
  from mc.config.wizard import run_setup_wizard

  config_mgr = ConfigManager()
  if not config_mgr.exists():
      print("No config file found. Running setup wizard...")
      config = run_setup_wizard()
      config_mgr.save(config)
  else:
      config = config_mgr.load()

  base_dir = config["base_directory"]
  offline_token = config["api"]["offline_token"]
  ```
- Pass config or specific values to command handlers as needed

Keep base directory existence check (line 16-18) but use loaded base_dir.
  </action>
  <verify>
# Test legacy detection
MC_BASE_DIR=/old/path python -c "from mc.cli.main import check_legacy_env_vars; check_legacy_env_vars()" 2>&1 | grep -q "Legacy environment variables detected"

# Test config loading path
python -c "from mc.cli.main import main; import sys; sys.argv = ['mc', '--help']; main()" | grep -q "mc"
  </verify>
  <done>
- check_legacy_env_vars() function detects MC_BASE_DIR and RH_API_OFFLINE_TOKEN
- Shell-specific unset instructions generated (bash/zsh vs fish)
- Hardcoded base_dir removed from main.py
- Duplicate offline_token check removed from main.py
- Config loaded via ConfigManager on startup
- Setup wizard runs automatically if config missing
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove duplicate validation from auth.py and write tests</name>
  <files>
    src/mc/utils/auth.py
    tests/unit/test_config.py
  </files>
  <action>
Update src/mc/utils/auth.py:
- Remove lines 18-21 (duplicate RH_API_OFFLINE_TOKEN check)
- get_access_token() should accept offline_token as parameter (passed from main)
- Update function signature: get_access_token(offline_token: str) -> str
- Remove os.environ.get('RH_API_OFFLINE_TOKEN') call
- Use passed offline_token parameter directly

Create tests/unit/test_config.py:
- Test ConfigManager.get_config_path() returns Path with "mc" in it
- Test ConfigManager.exists() returns False when no file
- Test ConfigManager.save() and load() round-trip
- Test config saved in binary mode (tomli_w requirement)
- Test wizard with mocked input() calls (patch builtins.input)
- Test wizard requires offline_token (empty input loops)
- Test wizard uses defaults when user input is empty for base_directory
- Test legacy env var detection with monkeypatch (both vars, bash shell)
- Test legacy env var detection with fish shell
- Use tmp_path fixture for config file testing
- Use monkeypatch for environment variable testing

All tests should use monkeypatch and tmp_path from pytest fixtures (established in Phase 1).
  </action>
  <verify>
pytest tests/unit/test_config.py -v --tb=short
pytest tests/unit/test_auth.py -v (verify existing tests still pass with new signature)
  </verify>
  <done>
- Duplicate offline_token validation removed from auth.py
- get_access_token() accepts offline_token parameter
- test_config.py created with comprehensive config system tests
- All config tests pass (load/save round-trip, wizard, legacy detection)
- Existing auth tests still pass with updated signature
  </done>
</task>

</tasks>

<verification>
Run full test suite to ensure config migration doesn't break existing functionality:
```bash
pytest tests/ -v
```

Test legacy env var detection manually:
```bash
MC_BASE_DIR=/old/path mc --help  # Should fail with migration instructions
```

Test wizard on fresh system:
```bash
rm ~/.config/mc/config.toml
mc --help  # Should trigger wizard
```

Verify config file created at correct location:
```bash
ls ~/.config/mc/config.toml  # Linux
ls ~/Library/Application\ Support/mc/config.toml  # macOS
```
</verification>

<success_criteria>
- TOML config file system working (load/save/exists)
- Config location uses platformdirs (XDG on Linux, proper paths on macOS/Windows)
- Interactive wizard runs on first use with sensible defaults
- Legacy env var detection fails immediately with helpful migration messages
- Shell-specific unset commands generated (bash/zsh vs fish)
- Duplicate offline_token validation removed from codebase
- All tests pass including new config system tests
- Requirements DEBT-01 and DEBT-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-cleanup/03-01-SUMMARY.md`
</output>
