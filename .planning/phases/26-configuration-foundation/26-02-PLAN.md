---
phase: 26-configuration-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/unit/test_config.py
autonomous: true

must_haves:
  truths:
    - "Tests verify [version] section defaults (pinned_mc='latest', last_check=None)"
    - "Tests verify backward compatibility (configs without [version] section still work)"
    - "Tests verify atomic write safety (temp file cleanup, os.replace usage)"
    - "Tests verify partial updates preserve other fields"
    - "All new tests pass"
  artifacts:
    - path: "tests/unit/test_config.py"
      provides: "Comprehensive test coverage for version config functionality"
      contains: "TestVersionConfig"
      min_lines: 300
  key_links:
    - from: "tests/unit/test_config.py"
      to: "src/mc/config/manager.py"
      via: "ConfigManager import"
      pattern: "from mc\\.config\\.manager import ConfigManager"
    - from: "test_version_config_defaults"
      to: "ConfigManager.get_version_config"
      via: "method invocation"
      pattern: "get_version_config\\(\\)"
---

<objective>
Create comprehensive test suite for version config functionality, verifying backward compatibility, atomic write safety, and correct default handling.

Purpose: Ensure the new version config infrastructure works correctly across all scenarios (missing config, missing [version] section, partial updates, atomic writes). Tests serve as living documentation and regression prevention.

Output: Extended test suite with new TestVersionConfig class covering all version management scenarios.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-configuration-foundation/26-CONTEXT.md
@.planning/phases/26-configuration-foundation/26-RESEARCH.md

# Implementation under test
@src/mc/config/manager.py
@src/mc/config/models.py

# Existing test patterns
@tests/unit/test_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for version config models</name>
  <files>tests/unit/test_config.py</files>
  <action>
Add test class TestVersionConfig to tests/unit/test_config.py with tests for version-related functionality.

Tests for models (get_default_config and validate_config):

1. test_default_config_includes_version_section:
   - Call get_default_config()
   - Assert 'version' key exists
   - Assert config['version']['pinned_mc'] == 'latest'
   - Assert config['version']['last_check'] is None

2. test_validate_config_accepts_version_section:
   - Create config with [version] section (pinned_mc="2.0.4", last_check=1234567890.0)
   - Assert validate_config() returns True

3. test_validate_config_accepts_missing_version_section:
   - Create config without [version] section (backward compatibility)
   - Assert validate_config() returns True

4. test_validate_config_rejects_invalid_version_types:
   - Create config with version.pinned_mc as integer (should be string)
   - Assert validate_config() returns False
   - Create config with version.last_check as string (should be float/None)
   - Assert validate_config() returns False

Follow existing test patterns in the file. Use pytest fixtures (tmp_path). Keep tests focused and independent.
  </action>
  <verify>
Run: pytest tests/unit/test_config.py::TestVersionConfig -v

Expected output shows all version config model tests passing.
  </verify>
  <done>
TestVersionConfig class exists with tests for default config and validation.
Tests verify [version] section structure, defaults, and backward compatibility.
Tests verify type validation for version fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for version config manager methods</name>
  <files>tests/unit/test_config.py</files>
  <action>
Add tests to TestVersionConfig class for ConfigManager version helper methods and atomic writes.

Tests for ConfigManager methods:

5. test_get_version_config_returns_defaults_when_missing:
   - Create ConfigManager with tmp_path (no config file)
   - Call get_version_config()
   - Assert returns {'pinned_mc': 'latest', 'last_check': None}

6. test_get_version_config_returns_stored_values:
   - Create ConfigManager, set config path to tmp_path
   - Save config with [version] section (pinned_mc="2.0.4", last_check=1234567890.0)
   - Call get_version_config()
   - Assert returns correct values from file

7. test_update_version_config_creates_section_if_missing:
   - Create ConfigManager with empty config file
   - Call update_version_config(pinned_mc="2.0.4", last_check=time.time())
   - Load config and assert [version] section exists with correct values

8. test_update_version_config_partial_update_preserves_fields:
   - Create ConfigManager, save config with both fields set
   - Call update_version_config(pinned_mc="2.0.5") (only update pinned_mc)
   - Assert pinned_mc changed but last_check unchanged

9. test_save_atomic_creates_file:
   - Create ConfigManager with tmp_path
   - Call save_atomic({'test': 'value'})
   - Assert file exists and content correct

10. test_save_atomic_no_temp_file_left_on_success:
    - Create ConfigManager with tmp_path
    - Call save_atomic({'test': 'value'})
    - List tmp_path directory
    - Assert no .config_*.tmp files exist (cleanup successful)

11. test_save_atomic_overwrites_existing_file:
    - Create ConfigManager, save initial config
    - Call save_atomic with different config
    - Assert file content reflects new config (atomic replace worked)

Use time.time() for timestamp tests. Import time module at top. Follow existing test patterns. Use tmp_path fixture for isolated test environments.
  </action>
  <verify>
Run: pytest tests/unit/test_config.py::TestVersionConfig -v

Expected output shows all TestVersionConfig tests passing (11 tests total).
  </verify>
  <done>
Tests verify get_version_config() backward-compatible defaults.
Tests verify update_version_config() creates [version] section and does partial updates.
Tests verify save_atomic() creates files, cleans up temp files, and overwrites atomically.
All 11 tests in TestVersionConfig pass.
  </done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
pytest tests/unit/test_config.py -v
```

Expected output:
- All existing tests still pass (no regressions)
- All new TestVersionConfig tests pass (11 tests)
- Test coverage includes backward compatibility scenarios
- Test coverage includes atomic write safety

Check test count:
```bash
pytest tests/unit/test_config.py --collect-only | grep "test session starts" -A 50
```

Expected: Should show 11 new tests under TestVersionConfig class.
</verification>

<success_criteria>
- TestVersionConfig class exists with 11 tests covering version config functionality
- Tests verify default config includes [version] section with correct defaults
- Tests verify backward compatibility (configs without [version] work)
- Tests verify get_version_config() returns correct defaults when section/fields missing
- Tests verify update_version_config() creates section and does partial updates
- Tests verify save_atomic() creates files and cleans up temp files
- All tests pass: pytest tests/unit/test_config.py::TestVersionConfig returns 11/11 passing
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/26-configuration-foundation/26-02-SUMMARY.md` following the standard summary template.
</output>
