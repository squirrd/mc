---
phase: 30-mc-update-core
plan: 02
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - tests/unit/test_update.py
autonomous: true

must_haves:
  truths:
    - "When uv is not on PATH, _run_upgrade() returns 1 and stderr contains 'uv not found'"
    - "When mc is not on PATH after upgrade, _verify_mc_version() returns False and stderr contains 'mc not found on PATH'"
    - "When upgrade succeeds but mc --version exits non-zero, upgrade() returns 1 with recovery instructions"
    - "mc-update upgrade blocked in agent mode with clear message — no uv subprocess is ever called"
    - "main() with no subcommand prints help and exits 0 — does not raise unhandled exception"
  artifacts:
    - path: "tests/unit/test_update.py"
      provides: "Full edge case coverage: FileNotFoundError paths, post-upgrade mc failure, agent mode guard, argparse help path"
  key_links:
    - from: "tests/unit/test_update.py"
      to: "mc.update._run_upgrade"
      via: "patch mc.update.subprocess.run with FileNotFoundError side_effect"
      pattern: "FileNotFoundError"
    - from: "tests/unit/test_update.py"
      to: "mc.update.upgrade"
      via: "monkeypatch MC_RUNTIME_MODE=agent then call upgrade()"
      pattern: "MC_RUNTIME_MODE.*agent"
---

<objective>
Expand `tests/unit/test_update.py` to cover all edge cases and failure modes from the research pitfall analysis. This plan ensures the failure handling paths — FileNotFoundError on uv, FileNotFoundError on mc, post-upgrade mc broken, agent mode guard, argparse no-command path — have dedicated test coverage.

Purpose: Phase 30 success criteria require that failure paths are explicitly verified. Tests written in 30-01 cover happy path and upgrade failure. This plan covers the remaining edge cases that the research identified as real-world pitfalls.

Output: Expanded `tests/unit/test_update.py` with complete edge case tests. No new source files — only test additions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-mc-update-core/30-01-SUMMARY.md
@.planning/phases/30-mc-update-core/30-RESEARCH.md
@src/mc/update.py
@tests/unit/test_update.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FileNotFoundError and broken-mc edge case tests</name>
  <files>tests/unit/test_update.py</files>
  <action>
Read the current `tests/unit/test_update.py` (written in 30-01) to understand existing test structure. Then add the following tests, inserting them into the correct existing test classes without disturbing existing tests.

Add to **class TestRunUpgrade** (if not already present):
- `test_run_upgrade_uses_list_form_not_shell`: patch `mc.update.subprocess.run` to return `MagicMock(returncode=0)`. After calling `_run_upgrade()`, assert the call args used list form: `mock_run.call_args[0][0] == ['uv', 'tool', 'upgrade', 'mc']`. Also assert `shell` keyword was not set to True (subprocess security: always list form, never shell=True). Check: `mock_run.call_args.kwargs.get('shell', False) is False`.

Add to **class TestVerifyMcVersion** (if not already present):
- `test_verify_mc_not_found_stderr_message`: patch `mc.update.subprocess.run` to raise `FileNotFoundError`. Call `_verify_mc_version()`. Assert return value is False AND `capsys.readouterr().err` contains `"mc not found on PATH"`. This validates the specific user-facing message from RESEARCH Pitfall 2.

Add to **class TestUpgrade**:
- `test_upgrade_verify_step_fails_after_success`: This tests RESEARCH Pitfall 3 (upgrade "succeeds" but mc is broken). Patch `mc.update.subprocess.run` with `side_effect` list: upgrade call returns `MagicMock(returncode=0)`, version call returns `MagicMock(returncode=1, stdout="error: something went wrong\n")`. Call `upgrade()`. Assert return is 1 AND `capsys.readouterr().err` contains `"uv tool install --force mc"`. Also assert stderr does NOT contain "Upgrade complete" (the success message must not appear when verify fails).
- `test_upgrade_agent_mode_does_not_call_uv`: patch `mc.update.subprocess.run` (to detect if it gets called) AND monkeypatch `MC_RUNTIME_MODE=agent`. Call `upgrade()`. Assert `mock_run.call_count == 0` — the agent mode guard must fire before any subprocess call.

Add new **class TestPrintRecoveryInstructions**:
- `test_recovery_instructions_content`: call `_print_recovery_instructions()` directly. Capture with `capsys`. Assert `capsys.readouterr().err` contains `"uv tool install --force mc"` and contains `"To recover"`. This verifies the exact recovery message text required by UPDATE-03.

After all additions, run `uv run pytest tests/unit/test_update.py -v` and confirm all tests pass. There should be no changes to existing passing tests — only additions.
  </action>
  <verify>
    uv run pytest tests/unit/test_update.py -v --tb=short
  </verify>
  <done>
    All tests in `tests/unit/test_update.py` pass (the original 12 from 30-01 plus the 5 new tests = 17 total, or close to it depending on exact deduplication with 30-01 tests).
    `test_upgrade_agent_mode_does_not_call_uv` confirms the agent guard fires before subprocess.
    `test_verify_mc_not_found_stderr_message` confirms the exact PATH error message.
    `test_upgrade_verify_step_fails_after_success` confirms recovery instructions appear when mc is broken post-upgrade.
    Exit code 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final integration check — linting, type check, full test suite</name>
  <files></files>
  <action>
Run the full quality gate sequence for Phase 30 to confirm the complete implementation passes all checks.

Execute in order:

1. `uv run mypy src/mc/update.py` — type checking must exit 0 with no errors

2. `uv run flake8 src/mc/update.py tests/unit/test_update.py` — linting must exit 0 with no errors (line length 100, codebase standard)

3. `uv run pytest tests/unit/test_update.py -v` — all update tests must pass

4. `uv run pytest -m "not integration" --tb=short -q` — full unit suite must pass with no regressions; coverage must remain above 60% (configured minimum in pyproject.toml)

If any step fails, fix the issue in the relevant file before proceeding to the next check. Common issues to watch for:
- mypy: missing return type annotations, wrong Literal type usage, `Any` sneaking in through MagicMock in tests (use `# type: ignore` sparingly, only when unavoidable in test files)
- flake8: E501 (line too long — keep to 100), F401 (unused imports), W291 (trailing whitespace)
- pytest: import errors if `from mc.update import ...` paths are wrong relative to `src/` layout (project uses `--import-mode=importlib` so this should be fine)

Do NOT modify test logic to make tests pass artificially — fix the source if source is wrong, fix the test if the test assertion is wrong.
  </action>
  <verify>
    uv run mypy src/mc/update.py && uv run flake8 src/mc/update.py tests/unit/test_update.py && uv run pytest -m "not integration" --tb=short -q
  </verify>
  <done>
    All three commands exit 0.
    No mypy errors on `src/mc/update.py`.
    No flake8 violations on source or test file.
    Full unit suite passes, coverage at or above 60%.
    Phase 30 implementation complete.
  </done>
</task>

</tasks>

<verification>
Confirm the complete Phase 30 outcome:

```bash
# All quality checks
uv run mypy src/mc/update.py
uv run flake8 src/mc/update.py tests/unit/test_update.py
uv run pytest tests/unit/test_update.py -v

# Full suite no regressions
uv run pytest -m "not integration" -q

# Entry point registered
uv run python -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert 'mc-update' in d['project']['scripts']; print('Entry point ok:', d['project']['scripts']['mc-update'])"
```

Phase 30 success criteria verified:
- [ ] UPDATE-01: `mc-update upgrade` runs `uv tool upgrade mc` (tested in TestRunUpgrade)
- [ ] UPDATE-02: Post-upgrade `mc --version` is called and output printed (tested in TestUpgrade.test_upgrade_success)
- [ ] UPDATE-03: Failure prints `uv tool install --force mc` to stderr (tested in TestPrintRecoveryInstructions + TestUpgrade)
- [ ] Agent mode guard: upgrade() exits 1 without calling uv (tested in test_upgrade_agent_mode_does_not_call_uv)
- [ ] Entry point: `mc-update = "mc.update:main"` in pyproject.toml
</verification>

<success_criteria>
1. `uv run mypy src/mc/update.py` exits 0
2. `uv run flake8 src/mc/update.py tests/unit/test_update.py` exits 0
3. All update unit tests pass — including all edge case tests added in this plan
4. `test_upgrade_agent_mode_does_not_call_uv` confirms zero subprocess calls when agent mode active
5. `test_recovery_instructions_content` confirms `uv tool install --force mc` appears in recovery output
6. Full unit suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/30-mc-update-core/30-02-SUMMARY.md` using the summary template.
</output>
