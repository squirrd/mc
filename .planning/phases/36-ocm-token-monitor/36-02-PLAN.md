---
phase: 36-ocm-token-monitor
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - src/mc/cli/main.py
autonomous: true

must_haves:
  truths:
    - "start_background_monitor() is called in main.py inside the if get_runtime_mode() != 'agent' block"
    - "OCM monitor call is placed immediately after the show_update_banner() try/except block"
    - "main.py uses a lazy import (inside try block) matching the existing import style in the file"
    - "No mc command is delayed or blocked — daemon thread, not synchronous wait"
    - "uv run mc --help exits normally without hanging"
  artifacts:
    - path: "src/mc/cli/main.py"
      provides: "OCM monitor integration hook"
      contains: "start_background_monitor"
  key_links:
    - from: "src/mc/cli/main.py"
      to: "src/mc/utils/ocm_monitor.py"
      via: "from mc.utils.ocm_monitor import start_background_monitor"
      pattern: "start_background_monitor"
---

<objective>
Wire start_background_monitor() into src/mc/cli/main.py so the OCM token monitor runs on every host-mode mc invocation.

Purpose: Activate the OCM monitor by hooking it into the existing host-only block in main() — the same guard that controls show_update_banner(). One small diff to main.py is the entire scope of this plan.
Output: Modified src/mc/cli/main.py with the OCM monitor call in the correct location, matching existing style exactly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-ocm-token-monitor/36-CONTEXT.md
@.planning/phases/36-ocm-token-monitor/36-RESEARCH.md
@.planning/phases/36-ocm-token-monitor/36-01-SUMMARY.md

@src/mc/cli/main.py
@src/mc/utils/ocm_monitor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OCM monitor call to main.py</name>
  <files>src/mc/cli/main.py</files>
  <action>
Locate the update banner block in main() (around line 150-155):

```python
        # Show update banner (foreground check, once per calendar day, suppressed for --version)
        if get_runtime_mode() != 'agent':
            try:
                show_update_banner()
            except Exception as e:
                logger.debug("Update banner failed: %s", e)
```

Add the OCM monitor call immediately after that block and before the `# Route to appropriate command` comment. The call must be inside the SAME `if get_runtime_mode() != 'agent':` block structure (i.e., at the same nesting level as the banner try/except — not inside the existing if block but immediately after it as a new if block). Use a lazy import inside the try block, matching the existing patterns in the file:

```python
        # OCM token monitor (host-only, daemon thread)
        if get_runtime_mode() != 'agent':
            try:
                from mc.utils.ocm_monitor import start_background_monitor
                start_background_monitor()
            except Exception as e:
                logger.debug("OCM monitor failed: %s", e)
```

Do NOT:
- Add any top-level imports for ocm_monitor
- Modify the existing show_update_banner block
- Change any other code in main.py
- Combine the two if blocks (keep them separate — this preserves the existing structure)

The result should be two separate `if get_runtime_mode() != 'agent':` blocks adjacent to each other, which is intentional and consistent with how the codebase is structured (each startup concern is independently guarded).
  </action>
  <verify>
    uv run mc --help
  </verify>
  <done>
    `uv run mc --help` completes and exits (does not hang). The string "start_background_monitor" appears in src/mc/cli/main.py. `uv run mypy src/mc/cli/main.py` reports zero new errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    OCM monitor integrated into mc CLI startup. When any mc command runs on the host:
    - If ocm.json is absent: cyan info message printed to stderr
    - If token expires within 60 min: yellow warning printed to stderr + ocm login triggered in background
    - If token is fresh: silent, no visible output
    - All other mc commands proceed normally without delay
  </what-built>
  <how-to-verify>
    1. Run `uv run mc --help` — should print help and exit cleanly (no hang)
    2. If you have a real ocm.json at the expected path (~/.config/ocm/ocm.json on Linux, ~/Library/Application Support/ocm/ocm.json on macOS):
       - Run `uv run mc --help` — should either be silent (token fresh) or show a yellow warning (token expiring)
    3. If you do NOT have ocm.json:
       - Run `uv run mc --help` — should see a cyan info line on stderr: "OCM config not found at ... — run 'ocm login' to set up"
    4. Confirm the output appears before the mc help text (startup message, not appended after)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
After Task 1 completes:
1. `uv run mc --help` exits without hanging
2. `grep -n "start_background_monitor" src/mc/cli/main.py` shows the call is present
3. `uv run mypy src/mc/cli/main.py` — zero new errors
4. `uv run pytest tests/unit/ -m "not integration" -q` — no regressions
</verification>

<success_criteria>
- src/mc/cli/main.py contains the OCM monitor call in the host-only block
- `uv run mc --help` completes normally (no hang, no uncaught exceptions)
- When ocm.json is absent: info message visible on stderr during any mc command
- Zero mypy errors introduced
- Zero regressions in existing unit test suite
- Phase 36 success criteria 1-6 all satisfied end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/36-ocm-token-monitor/36-02-SUMMARY.md`
</output>
