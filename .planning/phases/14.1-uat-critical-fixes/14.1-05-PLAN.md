---
phase: 14.1-uat-critical-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/UAT-TEST-PLAN.md
autonomous: true

must_haves:
  truths:
    - "UAT test plan references correct command: mc container list"
    - "No references to incorrect mc container ls command"
  artifacts:
    - path: ".planning/UAT-TEST-PLAN.md"
      provides: "Accurate UAT test procedures"
      contains: "mc container list"
  key_links: []
---

<objective>
Fix UAT test plan documentation - correct command is "mc container list" not "mc container ls".

Purpose: Fix documentation bug - test plan references incorrect command that doesn't exist
Output: UAT test plan accurately reflects actual CLI commands
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Documentation to fix
@.planning/UAT-TEST-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Replace mc container ls with mc container list in UAT test plan</name>
  <files>.planning/UAT-TEST-PLAN.md</files>
  <action>
Search and replace all instances of "mc container ls" with "mc container list" in .planning/UAT-TEST-PLAN.md.

Known instances:
- Line 139: Test 2.2 code block
- Line 159: Test 2.3 code block (appears twice)

Use case-sensitive search to avoid affecting unrelated text.

Search pattern: `mc container ls`
Replace with: `mc container list`

Verify the command subcommand is "list" by checking:
```bash
grep -n "def list_containers" src/mc/cli/commands/container.py
grep -n "container_subparsers.add_parser.*list" src/mc/cli/main.py
```

This confirms the correct command is "mc container list" not "mc container ls".
  </action>
  <verify>
Verify replacement:
```bash
# Should find multiple instances of correct command
grep -n "mc container list" .planning/UAT-TEST-PLAN.md

# Should find NO instances of incorrect command
grep -n "mc container ls" .planning/UAT-TEST-PLAN.md
# Expected: no output (0 matches)
```

Test actual command works:
```bash
mc container list --help
# Should show help for list command

mc container ls --help 2>&1
# Should show "invalid choice: 'ls'" error
```
  </verify>
  <done>
- All references to "mc container ls" replaced with "mc container list"
- UAT test plan accurately reflects CLI commands
- Test procedures will execute correctly when followed
  </done>
</task>

</tasks>

<verification>
Manual review:
```bash
# Count fixes
grep -c "mc container list" .planning/UAT-TEST-PLAN.md
# Expected: 3+ occurrences

# Verify no incorrect references remain
grep "mc container ls" .planning/UAT-TEST-PLAN.md
# Expected: no output

# Test command
mc container list
# Should work (shows containers or "No containers found")
```
</verification>

<success_criteria>
- UAT test plan uses correct command: mc container list
- No references to incorrect mc container ls command
- Test procedures are executable as written
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-uat-critical-fixes/14.1-05-SUMMARY.md`
</output>
