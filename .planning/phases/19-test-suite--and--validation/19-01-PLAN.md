---
phase: 19-test-suite--and--validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_terminal_attach.py
  - tests/unit/test_terminal_shell.py
  - tests/unit/test_cli_container_commands.py
  - tests/unit/test_platform_detect.py
  - tests/unit/test_container_manager_create.py
  - tests/unit/test_version.py
autonomous: true

must_haves:
  truths:
    - "All 10 failing unit tests now pass"
    - "Test suite completes with 0 failures"
    - "Coverage remains at or above 74%"
  artifacts:
    - path: "tests/unit/test_terminal_attach.py"
      provides: "Updated tests for window title format changes"
      min_lines: 50
    - path: "tests/unit/test_terminal_shell.py"
      provides: "Updated tests for consolidated directory structure"
      min_lines: 20
  key_links:
    - from: "tests/unit/test_terminal_attach.py"
      to: "src/mc/terminal/attach.py"
      via: "build_window_title() format validation"
      pattern: "def test_build_window_title"
---

<objective>
Fix 10 failing unit tests to achieve clean test suite with 0 failures.

Purpose: Ensure all tests pass after implementation changes in phases 15-18 (window tracking, registry, consolidated paths). Tests are failing due to outdated assertions, not bugs in production code.
Output: Green test suite (100% pass rate), coverage maintained at 74%+
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-test-suite--and--validation/19-CONTEXT.md
@.planning/phases/19-test-suite--and--validation/19-RESEARCH.md

# Implementation changes that broke tests
@.planning/phases/16-macos-window-tracking/16-02-SUMMARY.md
@.planning/INTEGRATION_TEST_FIX_REPORT.md

# Failing test files
@tests/unit/test_terminal_attach.py
@tests/unit/test_terminal_shell.py
@tests/unit/test_cli_container_commands.py
@tests/unit/test_platform_detect.py
@tests/unit/test_container_manager_create.py
@tests/unit/test_version.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix window title format test failures (3 tests)</name>
  <files>tests/unit/test_terminal_attach.py</files>
  <action>
Update test assertions in test_terminal_attach.py to match new window title format from Phase 16.

**Root cause:** Phase 16 changed title format from `{case} - {customer} - {description}` to `{case}:{customer}:{description}:/{vm-path}` (per INTEGRATION_TEST_FIX_REPORT.md). Unit tests still check old format.

**Failing tests:**
1. `test_build_window_title_format` - expects old " - " separators
2. `test_build_window_title_truncation` - expects old format in truncated output
3. `test_attach_terminal_window_title` - expects old format in launch options

**Changes needed:**
- Update expected format to use colon separators: `{case}:{customer}:{description}`
- Add vm_path component to expected strings: `:/{vm-path}` or `:/case`
- Update assertions checking for " - " to check for ":" instead
- Verify truncation logic works with new format (100 char limit preserved)

**Example fix:**
```python
# OLD assertion
assert title == "04347611 - IBM - Transfer ownership"

# NEW assertion
assert title == "04347611:IBM:Transfer ownership://case"
```

Do NOT change production code in src/mc/terminal/attach.py - tests need updating, not implementation.
  </action>
  <verify>uv run pytest tests/unit/test_terminal_attach.py -v</verify>
  <done>All 3 terminal_attach tests pass: test_build_window_title_format, test_build_window_title_truncation, test_attach_terminal_window_title</done>
</task>

<task type="auto">
  <name>Task 2: Fix consolidated directory structure test (1 test)</name>
  <files>tests/unit/test_terminal_shell.py</files>
  <action>
Update test_get_bashrc_path_uses_platformdirs to reflect consolidated ~/mc/config/ structure.

**Root cause:** Phase 14.1 consolidated directories from platformdirs to ~/mc/config/. Test still expects old platformdirs path (~/Library/Application Support/mc/ on macOS or ~/.local/share/mc/ on Linux).

**Failing test:** `test_get_bashrc_path_uses_platformdirs`

**Change needed:**
- Update expected path from `platformdirs.user_data_dir("mc", "redhat")` to `~/mc/config/bashrc`
- Or rename test to `test_get_bashrc_path_consolidated` if name is now misleading
- Verify get_bashrc_path() returns Path("~/mc/config/bashrc").expanduser()

**Example fix:**
```python
# OLD
expected = Path(platformdirs.user_data_dir("mc", "redhat")) / "bashrc"
assert result == expected

# NEW
expected = Path.home() / "mc" / "config" / "bashrc"
assert result == expected
```
  </action>
  <verify>uv run pytest tests/unit/test_terminal_shell.py::TestGetBashrcPath::test_get_bashrc_path_uses_platformdirs -v</verify>
  <done>test_get_bashrc_path_uses_platformdirs passes (or renamed test passes)</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining unit test failures (6 tests)</name>
  <files>
tests/unit/test_cli_container_commands.py
tests/unit/test_platform_detect.py
tests/unit/test_container_manager_create.py
tests/unit/test_version.py
  </files>
  <action>
Fix remaining 6 failing unit tests by updating assertions to match implementation changes.

**Failing tests:**
1. `test_cli_container_commands.py::TestCLIList::test_list_containers_success` (1 test)
2. `test_cli_container_commands.py::TestGetManager::test_get_manager_creates_manager` (1 test)
3. `test_container_manager_create.py::TestErrorHandling::test_image_not_found_raises_runtime_error` (1 test)
4. `test_platform_detect.py::TestGetSocketPath::test_get_socket_path_macos` (1 test)
5. `test_version.py::test_get_version_fallback_to_pyproject` (1 test, plus 1 duplicate)

**Analysis approach:**
For each failing test:
1. Run test individually with verbose output: `uv run pytest path/to/test.py::test_name -xvs`
2. Read error message to identify: Expected vs Actual
3. Determine if production code changed or test expectations are stale
4. Update test assertions (NOT production code) to match current implementation
5. Verify fix: Re-run test to confirm it passes

**Common patterns to check:**
- Path changes (platformdirs → ~/mc/)
- Error message changes
- Return value format changes
- Mock setup that no longer matches function signature

**Implementation:**
Fix tests one at a time, verifying each before moving to next. If any failure is due to actual bug (not stale test), document in verification output and treat as blocker.
  </action>
  <verify>uv run pytest tests/unit/test_cli_container_commands.py tests/unit/test_platform_detect.py tests/unit/test_container_manager_create.py tests/unit/test_version.py -v</verify>
  <done>All 6 tests pass, no failures in listed test files</done>
</task>

</tasks>

<verification>
Run full test suite to confirm all fixes:

```bash
# Run all tests
uv run pytest -v

# Verify specific outcomes
uv run pytest --tb=short | grep -E "(PASSED|FAILED|ERROR)"

# Confirm coverage still meets threshold
uv run pytest --cov=mc --cov-fail-under=60
```

Expected results:
- 521+ tests passed (was 511, now +10 from fixes)
- 0 tests failed (was 10, now fixed)
- 14 tests skipped (unchanged)
- Coverage: 74%+ (maintain or improve)
</verification>

<success_criteria>
- [ ] All 10 previously failing unit tests now pass
- [ ] Full test suite runs with 0 failures
- [ ] Coverage remains at 74% or higher (meets 60% threshold)
- [ ] No new test failures introduced by fixes
- [ ] test_duplicate_terminal_prevention_regression still passes (regression check)
</success_criteria>

<output>
After completion, create `.planning/phases/19-test-suite--and--validation/19-01-SUMMARY.md`
</output>
