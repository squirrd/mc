---
phase: 02-critical-path-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_auth.py
  - tests/unit/test_redhat_api.py
autonomous: true

must_haves:
  truths:
    - "Auth module validates missing token and returns clear error"
    - "Auth module successfully retrieves access token from SSO endpoint"
    - "RedHatAPIClient fetches case details with valid token"
    - "RedHatAPIClient handles HTTP errors (401, 403, 404, 500) with status codes"
    - "RedHatAPIClient lists attachments for a case"
  artifacts:
    - path: "tests/unit/test_auth.py"
      provides: "Auth module test coverage"
      min_lines: 80
    - path: "tests/unit/test_redhat_api.py"
      provides: "RedHatAPIClient test coverage"
      min_lines: 150
  key_links:
    - from: "tests/unit/test_auth.py"
      to: "mc.utils.auth.get_access_token"
      via: "direct import and invocation"
      pattern: "from mc\\.utils\\.auth import get_access_token"
    - from: "tests/unit/test_redhat_api.py"
      to: "responses library"
      via: "@responses.activate decorator"
      pattern: "@responses\\.activate"
    - from: "test assertions"
      to: "error messages"
      via: "pytest.raises with message validation"
      pattern: "with pytest\\.raises.*as exc_info"
---

<objective>
Create comprehensive unit tests for auth and API client modules using HTTP mocking.

Purpose: Establish test coverage for authentication and API communication, protecting against regressions in token management and API interactions. These are critical paths for all CLI operations.

Output: test_auth.py with 80%+ coverage and test_redhat_api.py with 80%+ coverage, using responses library for HTTP mocking.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-critical-path-testing/02-CONTEXT.md
@.planning/phases/02-critical-path-testing/02-RESEARCH.md
@src/mc/utils/auth.py
@src/mc/integrations/redhat_api.py
@tests/conftest.py
@tests/unit/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write auth module tests with error message validation</name>
  <files>tests/unit/test_auth.py</files>
  <action>
Create tests/unit/test_auth.py testing mc.utils.auth.get_access_token():

**Happy path test:**
- Use @responses.activate decorator
- Mock POST to mock_sso_url fixture with 200 response
- Response: {"access_token": "test_token_123", "expires_in": 3600}
- Assert returned token equals "test_token_123"
- Verify exactly one request made to SSO endpoint

**Error scenarios (with message validation per CONTEXT.md):**
- Test 1: Missing RH_API_OFFLINE_TOKEN environment variable
  - Use monkeypatch to delete env var
  - Capture SystemExit with pytest.raises
  - Capture stdout using io.StringIO (auth.py uses print())
  - Assert exit code is 1
  - Assert error message contains "RH_API_OFFLINE_TOKEN" and "must be set"

- Test 2: HTTP 401 unauthorized from SSO
  - Mock SSO endpoint returning 401 status
  - Use pytest.raises to catch requests.HTTPError
  - Verify response.status_code is 401

- Test 3: HTTP 500 server error from SSO
  - Mock SSO endpoint returning 500 status
  - Use pytest.raises to catch requests.HTTPError
  - Verify response.status_code is 500

**Use existing fixtures:**
- mock_env_vars (sets RH_API_OFFLINE_TOKEN)
- mock_sso_url (SSO endpoint URL)

**Pattern to follow (from RESEARCH.md):**
```python
import responses
import pytest
import io
import sys
from mc.utils.auth import get_access_token

@responses.activate
def test_get_access_token_success(mock_env_vars, mock_sso_url):
    # Implementation here
```

**Coverage target:** 80%+ for auth.py module
  </action>
  <verify>
Run: pytest tests/unit/test_auth.py -v
All tests pass, no failures
  </verify>
  <done>
test_auth.py exists with 4+ test functions covering happy path and 3 error scenarios. Error tests validate both exception types AND error message content. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write RedHatAPIClient tests with parametrized HTTP errors</name>
  <files>tests/unit/test_redhat_api.py</files>
  <action>
Create tests/unit/test_redhat_api.py testing mc.integrations.redhat_api.RedHatAPIClient:

**Happy path tests:**

1. test_fetch_case_details_success:
   - Use @responses.activate and api_client fixture
   - Mock GET {mock_api_base_url}/cases/12345678 returning realistic case data
   - Response: {"case_number": "12345678", "summary": "Test case", "status": "Open", "accountNumberRef": "1234567"}
   - Assert key fields match expected values
   - Verify Authorization header sent correctly

2. test_fetch_account_details_success:
   - Mock GET {mock_api_base_url}/accounts/1234567
   - Response: {"number": "1234567", "name": "Test Account"}
   - Assert account data returned correctly

3. test_list_attachments_success:
   - Mock GET {mock_api_base_url}/cases/12345678/attachments/
   - Response: [{"fileName": "sosreport.tar.gz", "link": "https://example.com/file1"}, {"fileName": "logs.txt", "link": "https://example.com/file2"}]
   - Assert attachment count is 2
   - Assert first fileName is "sosreport.tar.gz"

4. test_download_file_success (with tmp_path):
   - Mock GET to arbitrary URL with streaming response
   - Mock response with iter_content returning chunks
   - Use tmp_path to create destination file
   - Verify file exists and contains expected content

**Parametrized HTTP error tests (per CONTEXT.md - validate status codes):**

Use @pytest.mark.parametrize for HTTP errors:
```python
@pytest.mark.parametrize("status_code,error_msg", [
    (401, "Unauthorized"),
    (403, "Forbidden"),
    (404, "Not Found"),
    (500, "Internal Server Error"),
])
@responses.activate
def test_fetch_case_details_http_errors(api_client, mock_api_base_url, status_code, error_msg):
    # Mock endpoint returning error status
    # Verify HTTPError raised
    # Assert exc_info.value.response.status_code matches expected
```

Apply parametrized test to:
- fetch_case_details (404 = case not found common scenario)
- fetch_account_details
- list_attachments

**Use existing fixtures:**
- api_client (RedHatAPIClient with fake token)
- mock_api_base_url (base URL for mocking)
- tmp_path (for download test)

**Coverage target:** 80%+ for redhat_api.py module
  </action>
  <verify>
Run: pytest tests/unit/test_redhat_api.py -v
All tests pass, parametrized tests generate multiple test cases (4 per parametrized function)
  </verify>
  <done>
test_redhat_api.py exists with 7+ test functions (4 happy path + 3 parametrized error tests). HTTP error tests validate status codes. Download test uses tmp_path for real file creation. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify coverage targets and test execution</name>
  <files>tests/unit/test_auth.py, tests/unit/test_redhat_api.py</files>
  <action>
Run full test suite with coverage reporting:

1. Execute: pytest tests/unit/test_auth.py tests/unit/test_redhat_api.py --cov=mc.utils.auth --cov=mc.integrations.redhat_api --cov-report=term-missing -v

2. Verify coverage results:
   - auth.py: >= 80% coverage
   - redhat_api.py: >= 80% coverage

3. If coverage below target:
   - Identify uncovered lines from term-missing report
   - Add tests for critical uncovered branches (ignore trivial __init__ or defensive code)
   - Re-run coverage check

4. Verify all tests pass with no warnings

5. Check that existing placeholder test still passes: pytest tests/unit/test_placeholder.py
  </action>
  <verify>
Run: pytest tests/unit/ --cov=mc.utils.auth --cov=mc.integrations.redhat_api --cov-report=term-missing
Coverage report shows >= 80% for both modules
All tests pass (including placeholder test)
  </verify>
  <done>
Coverage meets targets (auth >= 80%, redhat_api >= 80%). All tests in tests/unit/ pass. Coverage report generated and shows covered/uncovered lines.
  </done>
</task>

</tasks>

<verification>
**Module coverage:**
- Run: pytest --cov=mc.utils.auth --cov=mc.integrations.redhat_api --cov-report=html
- Open htmlcov/index.html and verify >= 80% for both modules

**Test quality checks:**
- All test functions follow test_* naming convention
- Error tests validate error messages (not just exception types)
- HTTP tests use responses library with @responses.activate
- Parametrized tests generate multiple test IDs
- No real HTTP calls made (responses library intercepts all)

**Requirements satisfied:**
- TEST-04: Auth module unit tests ✓
- TEST-05: RedHatAPIClient unit tests with mocked requests ✓
</verification>

<success_criteria>
- tests/unit/test_auth.py exists with comprehensive coverage of get_access_token()
- tests/unit/test_redhat_api.py exists with comprehensive coverage of RedHatAPIClient methods
- Coverage >= 80% for mc.utils.auth module
- Coverage >= 80% for mc.integrations.redhat_api module
- All tests pass when run with pytest tests/unit/
- Error message validation included in all error scenario tests
- HTTP status codes validated in parametrized error tests
- No real HTTP calls made (verified by responses library usage)
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-path-testing/02-01-SUMMARY.md`
</output>
